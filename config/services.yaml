# config/services.yaml
services:
    _defaults:
        autowire: true
        autoconfigure: true

    # Ajoutez ici vos services spécifiques si nécessaire

    App\:
        resource: '../src/*'
        exclude: '../src/{DependencyInjection,Entity,Migrations,Tests,Kernel.php}'
